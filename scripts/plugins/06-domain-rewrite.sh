#!/usr/bin/env bash
# 按需修改 OLD_DOMAIN / NEW_DOMAIN 后取消下方注释启用
# OLD_DOMAIN="oldsite.com"
# NEW_DOMAIN="junge.中国"
# ROOT_DIR="${1:-.}"
# echo "[06] 旧域名替换（$OLD_DOMAIN → $NEW_DOMAIN）"
# grep -rl "$OLD_DOMAIN" "$ROOT_DIR" --include="*.html" 2>/dev/null | while read -r file; do
#   sed -i "s#$OLD_DOMAIN#$NEW_DOMAIN#g" "$file"
# done
