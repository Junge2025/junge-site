#!/usr/bin/env bash
# 按需修改 OLD_CDN / NEW_CDN 后取消下方注释启用
# OLD_CDN="cdn.old.com"
# NEW_CDN="cdn.new.com"
# ROOT_DIR="${1:-.}"
# echo "[05] CDN 域名替换（$OLD_CDN → $NEW_CDN）"
# grep -rl "$OLD_CDN" "$ROOT_DIR" --include="*.html" 2>/dev/null | while read -r file; do
#   sed -i "s#$OLD_CDN#$NEW_CDN#g" "$file"
# done
